---
name: daily-magic-puzzle
description: 《开心水族箱》「每日魔幻拼图」活动解法（含从主界面进入的导航路径）。4x4 棋盘 + 4 块拼图，整行/整列循环平移，把 4 块拼成正确的 2x2 即为通关，每天 4 点刷新（通关即停，不代选奖励）。当用户提到「拼图」「魔幻拼图」「每日拼图」时使用。含界面指纹、导航坐标、滑动语义、BFS 求解流程、完整坐标表。⚠️ 未落子前棋盘可能每 60 秒自动重随机，识别→求解→执行要快。
agent_created: true
---

# 每日魔幻拼图 · 4x4 环面滑动拼图

> 首次破解：2026-09-14，一轮全清，开心宝消耗 **0**。
> 2026-09-15：素材/编号流程脚本化（`scripts/piece_numbering.py`），首个收录图案「蛋糕鱼」。
> 2026-09-15 晚：查明**棋盘每 60 秒自动重随机**（见规则 8）——这是本活动最大的坑。
> 2026-09-16：**导航路径全自动走通**（主界面 → 游乐园 → 魔方 → 2x2 难度卡），
> 并读全选择页信息：**每天 4 点刷新**、三张卡 = 2x2(4块) / 3x3(9块) / 4x4(16块)。图案 #2：**炮弹鱼（绿）**。
> 2026-09-17：图案 #3「紫衣锦鲤」（我误起名「白鱼」→ 芝麻更正）。
> 2026-09-18：图案 #4 = **巧克力鱼（绿）**（芝麻 17:13 定名）。
> ⚠️ 我原先认不出这条鱼，按纪律先用**中性占位名 `图案_20260918`** 落盘，拿到正式名后已把
> **目录名 + 文件名 + 全部引用**一起改掉 —— 这就是红线 5 的标准流程（**宁可占位，不要自己编**）。
> ★ 本局首次把「滑动手势 ↔ 步骤」的映射写成公式（见第三节第 4 步），并确认 **`▶` 圆钮会污染左下块模板**。

上层通用流程（连接 / 截图 / 读界面纪律 / 红线）见主技能 `../../SKILL.md`。

## 〇、怎么进活动（2026-09-16 实测，三步）

```
主界面 ─① 点左下角「游乐园」(58, 505)─▶ 活动面板
      ─② 点第 1 行第 5 个「魔方」图标 (515, 452)─▶ 「每日魔幻拼图」弹窗
      ─③ 点最左那张「2x2」难度卡 (469, 335)─▶ 拼图页（未开始，中央有 ▶）
```

| 步骤 | 坐标 | 说明 |
|---|---|---|
| ① 游乐园 | `(58, 505)` | 摩天轮图标**本体**；别点它上面的金币气泡（y≈455），也别点下面「游乐园」文字 |
| ② 魔方 | `(515, 452)` | 活动面板**第 1 行第 5 个**；面板范围 ≈ x110~640 / y400~620 |
| ③ 2x2 卡 | `(469, 335)` | 三张难度卡中心 x ≈ 469 / 630 / 792，y ≈ 335 |
| ④ 开始 ▶ | `(658, 335)` | 拼图页中央的播放钮（2026-09-18 实测可用） |

活动面板图标排布（第 1 行 y≈452）：`电视 │ 海马 │ 橙甲虫 │ 乌龟 │ 魔方★ │ 圣诞老人`
（第 2 行 y≈558：`海螺 │ 五角星 │ 粉章鱼 │ 红鱼 │ 黄鱼 │ 报纸`）

> 标注好的参考图：`活动档案/拼图_每日魔幻拼图/导航/01~05_*.png` + 同目录 `README.md`。

### 怎么出来（回程，2026-09-16 实测）

| 步骤 | 操作 | 坐标 |
|---|---|---|
| ⑤ | 奖励窗左上 **X** | **`(379, 169)`** |
| ⑥ | **直接回到主鱼缸** —— 一步到位，不用再点返回箭头 | — |

- 实测：点 X 后**直接回主界面**（星星 30549304 → 30549904，XP x600 到账）。
- 奖励窗三卡 ≈ `(469,336) 金币x2000` / `(629,336) XP x600` / `(788,336) 折价买(付费)`；
  选中任一卡后原处出现「领取」≈ `(750, 347)`。
- 左上绿色返回箭头约 `(180, 169)` —— **未实测**（弹窗打开时优先点弹窗自己的 X，避免误触底层控件）。

**这一屏还回答了三个老问题**：
- 提示语原文：「每天4点刷出一组新的魔幻拼图，完成拼图即可获得折价买鱼的机会~快试试吧~」
  → **每天 4:00 刷新**。
- 三张卡 = **块数**（不是棋盘大小）：2x2 = 4 块、3x3 = 9 块、4x4 = 16 块；棋盘容器始终 4×4。
- 右下「购买新拼图 🎫x5」花钱不点；右上「🎫 x20」是票券余量。

## 一、界面指纹

| 位置 | 内容 |
|---|---|
| 背景 | 蓝色海底；左上绿色返回键；右上九宫格按钮 + 「票券 x20」 |
| 选择页 | 标题「每日魔幻拼图」+ 提示语 + 三张难度卡（2x2/3x3/4x4）+ 底部「购买新拼图」 |
| 棋盘 | 金边圆角方形，**4×4 = 16 格**，格距 ≈ 138 px，外框 ≈ x346~920 / y82~654 |
| 起始页 | 4 块拼图按**正确排布**排在中央 2×2，正中压一个金色 ▶ 圆钮（半径≈82）。⚠️ 这里是**缩略预览**，明暗/尺度都与拼块本体不同 |
| 打乱后 | 灰格是**一整层遮罩**（反光带连续跨格），被挖空处露出拼块 |
| 通关 | 红蜡封「完成」印章 + 彩带 → 约 3 秒后弹奖励窗 |
| 奖励窗 | 标题「每日魔幻拼图」；先「请选择一个奖励」3 选 1 → 选完变「领取」 |
| 奖励窗底部 | 「购买新拼图 🎫x5」= **花钱，不点** |
| 奖励第 3 项 | 🐟「折价买」= **花钱，不点**（第 1、2 项免费）；**该卡图标 = 当天拼图的那条鱼**，可反查图案身份 |

## 二、核心规则（全部实测）

1. **不是拖拼块** —— 是**整行 / 整列循环平移**：横滑某行 → 该行横移，右侧绕回左侧；纵滑某列同理。
2. **滑动方向 = 内容移动方向**（往右滑 = 内容往右走）。2026-09-18 用「5 步解全部命中」再次验证。
3. **滑动距离 = 平移格数**：138 px ≈ 1 格。
4. **目标**：4 块拼成正确的 2×2 图。**不必回中央**——任意位置的 2×2，只要相对排布对即可。
   （求解器默认锁中央 2×2，两种判定都能满足。）
5. 灰格是可互换的空位，忽略。
6. **拼块不会被旋转**（散开前后内容逐像素一致）。
7. 开局散块位置随机 → **每轮都要重新识别 + 重新求解**，不要背坐标。
8. ⚠️⚠️ **未落子之前，棋盘可能每 60 秒自动重随机一次。**
   - 证据（2026-09-15，logcat）：`MiniGameController::tryToRandNewGame() k = 0` 连续 12 次，
     `15:50:30 → 15:51:31 → … → 15:57:32 → … → 16:00:32 → 16:01:32 (k=1)` 后停止，**间隔精确 60 s**。
   - 后果：**「打乱后」的截图只有 ≤60 秒保质期**。拿它慢慢分析会得到**过期坐标**。
   - 纪律：**截图 → 识别 → 求解 → 执行，尽量一口气跑完**；中间别去写文档、别去翻档案。
   - 执行前**再截一帧复核**：不一致 → 触发了重随机 → **重新识别 + 重新 BFS**（BFS 毫秒级，别心疼）。
   - ⚠️ **2026-09-16 反常观测**：点开始后 97 秒（11:55:45 → 11:57:22）棋盘**完全没变**（逐格模板识别 diff=0.0，
     两帧文件大小也一样）。与 09-15 的 60 秒连击不符 → 触发/停止条件仍**待复验**。
   - ⚠️ **2026-09-18 新证据（倾向于"确实会随机"）**：点开始后 1 s 抓的帧（`pz_shuf2`）与约 4 分钟后抓的帧
     （`pz_check2`）逐块识别结果**完全不同**；但**再往后连抓 4 帧（间隔 1.5 s）结果又完全一致**。
     → 合理解释：**重随机只在"刚开局的一段时间内"发生，之后停止**。
   - **结论不变：永远按"截图会过期"安排流程**，复核一帧的成本远低于拼错重来。

## 三、求解流程（照做）

0. **先把命令准备好**，再截图——链路越短越好（规则 8）。
1. **截图** → 用 `../../scripts/piece_identify.py` 识别 4 块在哪：
   `python piece_identify.py shot.png --tpl-dir <拼块模板目录> --xs 431,570,709,847 --ys 150,288,427,565`
   - 若这一步是**新图案**（没有现成模板），先做「定正确排布 + 出模板」，见第 2 步与第五节。
2. **定正确排布 + 出新图案模板**（已脚本化，一条命令）：
   `python ../../scripts/piece_numbering.py --idle 未开始.png --shuffled 打乱后.png --xs 431,570,709,847 --ys 150,288,427,565 --out-dir <目录> --prefix <图案名> --label <图案名>`
   - **原理**：**未开始帧的中央 2×2 就是正确排布**，只是正中压着 ▶ 圆钮 → 用圆形掩膜挖掉；再拿 24 种排布跟它做 **NCC（归一化互相关）**。
   - ⚠️ **起始页那张图不能当「模板」**去匹配拼块（是缩略预览，明暗/尺度都不同），**但可以当「标准答案」**。
   - ⚠️ **别用 MAE 打分**：两帧亮度差会把信号淹掉（24 种排布全挤在 75~78）。
   - ⚠️ **判空判据**：脚本默认用 `彩色像素占比 + 灰度std/100`（拼块 vs 中性灰遮罩）。
     **别退回「饱和度均值」**——2026-09-16 的星空鱼块饱和度均值只有 55~70，被判成灰格，只找到 2/4 块。
     万一自动判块仍不对：`--cells "r1c2,r1c3,r3c4,r4c4"` 手动指定。
   - ⚠️ **`▶` 圆钮会污染「左下块」模板**（2026-09-18 首次明确）：▶ 位于中央 2×2 的**偏左下**位置，
     把 **3 号（左下）块**大半遮成灰色，只留一条黄绿边。
     后果：该块在识别时的 `diff` **天然偏高**（本局 47，其余 3 块仅 1.3~2.4）。
   - ★ **判定纪律（照这个来，别硬设 diff 阈值）**：
     **只要「其余 3 块 diff < 5 且互不冲突」，剩下的那一格就是它**——不要因为某一块 diff 大就重试。
     脚本 `pz_auto.py` 已按此放宽为「3 块小 diff 校验」。
   - 兜底 / 交叉验证：把候选排布拼出来看图——正确的那张是一条完整连贯的鱼。
   - ⚠️ 「未开始帧」和「打乱后帧」**必须是同一局**。
3. **BFS 求解**：
   `python ../../scripts/line_shift_solver.py --n 4 --at "A=2,2 B=3,2 C=4,2 D=4,4" --shape "TL=C TR=A BL=D BR=B"`
   （默认目标锁**中央 2×2**，这样「任意位置」和「回中央」两种判定都能满足。）
   - ⚠️ 求解器打印的「目标: 2x2 左上角 = (1, 1)」是**文案 bug**，实际算的是 `center`（n=4 → (2,2)）；
     校验方法：看它输出的**终局状态**是否落在 r2c2/r2c3/r3c2/r3c3。
4. **按解逐步 `input swipe`**（800~1000 ms），**每步或每 2~3 步截图复核**（用第 1 步的脚本再识别一次）。
   - ★ **手势映射公式（2026-09-18 首次明确验证，5 步全中）** —— **拖动式：手指方向 = 内容移动方向**：
     | 步骤 | 手势（XS = 431,570,709,847；YS = 150,288,427,565） |
     |---|---|
     | 行 r **左移 k** | `swipe(XS[3], YS[r-1], XS[3-k], YS[r-1])` |
     | 行 r **右移 k** | `swipe(XS[3-k], YS[r-1], XS[3], YS[r-1])` |
     | 列 c **上移 k** | `swipe(XS[c-1], YS[3], XS[c-1], YS[3-k])` |
     | 列 c **下移 k** | `swipe(XS[c-1], YS[3-k], XS[c-1], YS[3])` |
     （即：行操作固定用最右列 `c4` 作起点/终点，列操作固定用最下行 `r4`，跨 k 格距离滑，天然避开中心区域。）
   - 复核不符 → 大概率是自动重随机抢先了一步：**停手，回去第 1 步重新识别重算**。
5. **拼成即停**：出现「完成」印章 / 彩带（约 3 秒后弹奖励窗）→ 本次自动化结束。**不点奖励窗任何东西**（见红线）。

实测解：
- 2026-09-14 `A=鱼头 B=鱼身 C=鱼尾 D=水波`，起始 A(2,2) B(3,2) C(4,2) D(4,4)：
  `行2右1 → 行3右1 → 列2上1 → 行4右2 → 列2上1` = 5 步。
- 2026-09-15 蛋糕鱼，起始 P1(4,3) P2(2,2) P3(1,2) P4(3,2)：
  `行2右1 → 行3右1 → 行4左1 → 列2下2` = 4 步。
- 2026-09-16 炮弹鱼（绿），起始 P1(3,4) P2(1,2) P3(1,3) P4(4,4)：
  `行1左1 → 列1下1 → 列4上1 → 行3左1 → 列2下2 → 行2右2` = 6 步。
- 2026-09-17 紫衣锦鲤，起始 1号(3,2) 2号(1,4) 3号(1,1) 4号(1,2)：
  `行1左1 → 列1下2 → 列2上1 → 列3下1 → 列4下2 → 行3右2` = 6 步。
  ⚠️ NCC 0.855 / 次佳 0.755（**领先仅 0.100，信号弱**）→ 已人工看编号示意图确认为完整图案。
- 2026-09-18 巧克力鱼（绿），起始 1号(2,4) 2号(1,3) 3号(4,2) 4号(2,2)：
  `行2右1 → 列1下1 → 行3右1 → 列2上1 → 列3下1` = **5 步**，NCC 0.744（次佳 0.532，信号强）。
  验证帧识别结果：1号(2,2) 2号(2,3) 3号(3,2) 4号(3,3) = 中央 2×2 ✅

## 四、坐标表（1280×720 · MuMu）

**导航**：游乐园 `(58,505)` → 魔方 `(515,452)` → 2x2 卡 `(469,335)` → 开始 ▶ `(658,335)`

**棋盘**：
- 列中心 x：`c1=431, c2=570, c3=709, c4=847`
- 行中心 y：`r1=150, r2=288, r3=427, r4=565`
- **滑 1 格 = 138 px**；滑动时长 800~1000 ms（实测 900 ms 稳）
- 起始页 ▶ 开始钮 ≈ `(658, 335)`（旧记 640,357 略偏，2026-09-18 实测 658,335 一次命中）

**奖励窗**（⚠️ 仅供人工参考，脚本不要自动点；2026-09-16 复测）：
- 3 卡（卡面中心，y 一致）≈ `(469,336) 金币x2000` / `(629,336) XP x600` / `(788,336) 折价买(付费)`
- 「领取」≈ `(750, 347)`（选中任一卡后才出现）
- 退出：左上 **X** ≈ `(379, 169)` → **直接回主鱼缸**

## 五、素材与编号规范（每个新图案一份）

图案**每天刷新**，每个图案单独建目录，互不复用：

```
活动档案/拼图_每日魔幻拼图/拼块模板/<图案名>_<YYYYMMDD>/
    <图案名>_1.png ~ <图案名>_4.png      ← 拼块模板（138×138）
    <图案名>_编号示意图.png               ← 4 块拼合 + 编号标注（3x）
    原料_未开始.png / 原料_打乱后.png     ← 溯源原帧
    完成_拼好盘面.png / 完成_停在奖励窗.png
    README.md                           ← 编号表 + 实测解 + 复现命令
```

> 📦 **对外镜像（2026-09-18 起）**：整套素材（含 `导航/` 与首日参考帧）已镜像到本仓库
> **`assets/daily-magic-puzzle/`**，供其它项目直接取用（不随技能安装）。
> 该目录是**副本** —— 改素材请改 `活动档案/` 源目录，再整体重新镜像，别两边各改一份。

- **编号 = 正确排布的左→右、上→下**：`1=左上  2=右上  3=左下  4=右下`。
- 文件名**必须带图案名**（如「炮弹鱼_绿」「蛋糕鱼」——芝麻按名字挑模板）。
- ★ **图案名一律由芝麻定，且来源必须标注**：这个名字是**芝麻给的**还是**我编的**，
  要在 README 与下表里写清楚（下表已加「命名」列）。
  - ⚠️ **2026-09-17 双向教训（两个方向都算污染档案）**：
    - **正查**：「紫衣锦鲤」我认不出来，就自己起了个「白鱼」。**编一个"像样"的错名比留空更有害** ——
      它会顺着目录名 / 文件名 / 模板库 / 实测表一路传下去，越晚改越贵。
      → 认不出来就**裁图发给芝麻问**（截「未开始帧」中央 2×2 那张最清楚）；
      赶时间先建 **`图案_<YYYYMMDD>` 占位名**，拿到名字后**连目录带文件一起改**。
    - **反查**：紧接着我又把**芝麻命名的**「蛋糕鱼」「炮弹鱼（绿）」当成"我自己编的、未经确认"
      写进记忆、还要请芝麻核对 —— **把芝麻的记录算成我的猜测，同样是在毁档案**。
      → **改名前先查来源**：`Grep <图案名> .workbuddy/memory/`，日志里写过「芝麻命名」就是芝麻定的。
- 模板一律从**打乱后**的原帧裁；**138×138 满格裁切**，与游戏渲染 1:1。
- ⚠️ **裁切/识别必须用同一帧**，且该帧拍下后尽快完成识别（规则 8）。
- **MAA 硬要求**：RGB PNG、**无 alpha 通道**、分辨率与截图一致（1280×720）。
- MAA 阈值：图案内相似度高（如大面积星空）建议 0.85~0.9 起；内容差异大（蛋糕鱼）0.8 起。
- 换算：模板中心 (x,y) → 格号 `col = round((x-431)/138)+1`，`row = round((y-150)/138)+1`。

已收录图案：
| 日期 | 图案 | 命名（谁来定的） | 素材目录 | NCC |
|---|---|---|---|---|
| 2026-09-14 | ⏳ **未定名**（首日，早于命名流程） | **无** —— `A_鱼头`/`B_鱼身`/`C_绿鳍鱼尾`/`D_水波白旋` 是按画面内容取的**描述名**，不是游戏内图案名。经 **NCC 全矩阵比对**与后 4 套图案均不重合（最佳仅 0.07~0.27，噪声级）→ 确认是**独立的第 5 套**，不是任何一套的重复（09-18 判同） | `拼块模板/` 顶层 4 张 | — |
| 2026-09-15 | 蛋糕鱼 | **芝麻命名**（09-15 日志「芝麻命名为蛋糕鱼」） | `蛋糕鱼_20260915/` | 0.928（次佳 0.534） |
| 2026-09-16 | 炮弹鱼（绿） | **芝麻命名**（09-16 日志「本轮图案：炮弹鱼（绿）（芝麻命名）」） | `炮弹鱼_绿_20260916/` | 0.904（次佳 0.737） |
| 2026-09-17 | 紫衣锦鲤 | **芝麻命名**（我原先误起「白鱼」，已更正） | `紫衣锦鲤_20260917/` | 0.855（次佳 0.755，弱信号） |
| 2026-09-18 | **巧克力鱼（绿）** | **芝麻命名**（17:13；我原先认不出，先用占位名 `图案_20260918`，已改回） | `巧克力鱼_绿_20260918/` | 0.744（次佳 0.532，信号强） |

## 六、红线

- **通关即停，不碰奖励窗。** 拼成 2×2、看到「完成」印章/彩带后，**立刻结束并停下**，不选卡片、不点「领取」，交给用户自己决定。奖励 3 选 1 是玩家个人偏好，脚本无权替人做选择。
- 永远不点的：奖励第 3 项 🐟「折价买」、底部「购买新拼图 🎫x5」——**都要花钱**。

## 七、待复验

- [ ] **自动重随机的触发/停止条件**：`tryToRandNewGame` 的 `k` 从 0 跳到 1 是什么判据？
      09-15 观测「未落子时每 60 s 一次、12 次后停」；**09-16 测得点开始后 97 s 纹丝不动**；
      **09-17 又一次测得「截图后约 60~90 s 再抓帧，两帧逐块识别结果完全一致」（未随机）**；
      **09-18 测得「开局 1 s 帧 ≠ 4 分钟后帧」，但相隔 1.5 s 连抓 4 帧又完全一致**。
      → 合理解释：**只在开局后一段时间内随机、之后停止**（待验具体时长）。
      **⚠️ 但纪律不变：永远按「截图会过期」安排流程 —— 复核一帧的成本远低于拼错重来。**
- [x] ~~「每天自动刷新」的确切重置时间点~~ → **每天 4:00**（选择页提示语明写）。
- [ ] 右上「票券 x20」的用途（免费次数？买新拼图用？）。
- [ ] 一天能做几轮、是否只有 1 次奖励（4 点刷新意味着一天一组，但能否重复玩？）。
- [x] ~~更高难度分别是几块~~ → **2x2 = 4 块 / 3x3 = 9 块 / 4x4 = 16 块**（选择页明写）。
- [ ] 3x3 / 4x4 的棋盘格距与滑动语义是否与 4 块模式一致（很可能一致，待验）。
- [ ] **`▶` 遮挡左下块模板能否绕过**：可选方案 —— 用「打乱后帧里已归位的块」或「另一局的开局帧」重建 3 号模板；
      或干脆在 `piece_numbering.py` 里对 3 号块改用「掩掉 ▶ 灰色区域后」的模板（待评估）。
